<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.shopmoon.mapper.MemberMapper">
	
	<!-- 회원가입 -->
	<insert id="memberJoin">
		insert into member values (
		SEQ_MEMBER.NEXTVAL, #{memberId}, #{memberPw}, #{memberName}, #{memberPhone}, #{memberEmail}, #{memberPostNum}, #{memberAddr1}, #{memberAddr2}, 'Y', sysdate, '0'
		)	
	</insert>
	
	<!-- 아이디 중복 검사 -->
	<select id="idCheck" resultType="int">
		select count(*) from member where M_ID = #{memberId}
	</select>
	
	<!-- 회원 로그인 -->
	<select id="memberLogin" resultType="org.shopmoon.domain.MemberVO">
		select M_ID as memberId, M_PW as memberPw, M_NAME as memberName, M_AUTHORITY as memberAuthority from member where M_ID = #{memberId}
	</select>
</mapper>